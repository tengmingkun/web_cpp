1.ifndef 的参数为头文件的大写；
2.用static 变量保存路由；
3.ifndf 解决重复定义
4.静态成员 要在类外生命才可以使用；
5.模板必须写到.h文件

问题：postman无参时出错；
在postman时没有一直没有返回；因为GET也携带了body，所以强行GET不带body，post带body;
在找不到路由时，记得退出；


进展：
完成了头部解析；
实现了路由机制；静态成员变量+成员函数保存路由；Router::RegisterGET()直接注册；
实现了上下文机制：用户可以直接得到变量的参数，以及回复函数；

下一步计划：
长连接实现；（先把定时器写出来）
- 支持优雅关闭连接
日志系统
长连接定时器
json库